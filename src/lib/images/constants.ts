/** Résolution cible pour l'impression (templates PDF). */
export const PRINT_RESOLUTION_DPI = 300

/**
 * Qualité JPEG upload / conversion HEIC (0–1, API canvas & heic-to).
 * 0.98 = quasi sans perte visible.
 */
export const UPLOAD_JPEG_QUALITY = 0.98

/** Qualité JPEG pipeline serveur Sharp (0–100). */
export const PRINT_JPEG_QUALITY = 98

/** Miniatures UI uniquement — pas utilisé pour l'impression. */
export const THUMB_JPEG_QUALITY = 0.8
